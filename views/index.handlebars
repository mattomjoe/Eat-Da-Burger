<!-- Setup the index.handlebars to have the template that Handlebars can render onto. -->

<!-- Create a button in index.handlebars that will submit the user input into the database. -->

<p style="text-align:center;"><img src="assets/img/burger-drop.gif" height="250" width="250"></p>

<h1 style="text-align:center;">Eat-Da-Burger!</h1>



{{!-- <div class="row">
    <div class="col-sm-6">
        <h2>Burgers!</h2>
        <ul>
            {{#each burgers}}
                {{#unless devoured}}
                    {{this.id}} - {{this.burger_name}}
                    <button type="button" class="eatburger"
                {{/unless}}
            {{/each}}
        </ul>


    </div>



</div> --}}


<h2>Devoured!</h2>

{{!-- <ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burgers/burger-block devoured=false}}
        {{/if}}
    {{/each}}
</ul> --}}


<h2>Add-Da-Burger</h2>

<form class="create-form">

    <div class="form-group">
        <label><!--for="addburger"-->Burger name:</label>
        <input type="text" id="addburger" name="burger">
        <button type="submit">SUBMIT</button>
    </div>

    {{!-- <div class="form-group">
        <label for="devoured">Devour it!</label>
        <br>
        <input type="radio" name="devoured" value="1" checked>Devoured!
        <br>
        <input type="radio" name="devoured" value="0">Burger!
    </div> --}}
    
    
</form>